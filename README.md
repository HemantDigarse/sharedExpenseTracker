# sharedExpenseTracker
Full-stack shared expenses app built with Spring Boot + React + PostgreSQL.  Features smart CSV import with anomaly detection, multi-currency support,  and time-based group membership.
